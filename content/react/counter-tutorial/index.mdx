export const metadata = {
  author: 'vishal',
  date: '22 June, 2020',
  tags: ['react', 'typescript'],
  title: `Building simple react Counter component with TDD`,
  description: `I will try to build a simple react component which will help me
   understand how local state and props work`,
  urlPath: '/react/counter-tutorial'
}

Create an empty react component named `Counter.tsx` with the following code.

```tsx
import React from 'react'

const Counter: React.FC = () => {
  return <div></div>
}

export default Counter
```